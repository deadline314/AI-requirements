---
description: Read-only check that the Google Workspace MCP is configured and reachable. Safe to run anytime.
---

Run the preflight check and report the result to the user.

Read `${CLAUDE_PLUGIN_ROOT}/skills/preflight-check/SKILL.md` and follow it. Report `ready: true/false` plus the reason and the recommended next step. Do not classify any emails — this command is diagnostic only.
